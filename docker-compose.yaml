
# ! this is just to build service in src, deployment is done through terraform 

services:
  airflow-hydrosat-pdqueiros:
    build:
      context: .
      dockerfile: ./Dockerfile
    image: airflow-hydrosat-pdqueiros:latest
